---
title: "story"
description: "全体のストーリー下書き"
format: 
  html:
    toc: true
    number-sections: true
execute:
  echo: false
  freeze: auto
  warning: false
  message: false
---

```{r source}
# 前処理用ファイルをソースとする

source("kaminoko_2025_preparation.R")
```

# はじめに

## 自然観光研究の状況

-   purist指標
-   入域料　→　消費者として評価？　市民として評価？

## 神の子池の特殊性

-   アクセスも設備も悪いのに訪問が絶えない

## 目的

-   探索的に明らかにする

# ROSと入域料

## CVMを用いた入域料研究

# 調査概要

## 実施手順

-   6月8日、6月14-15日、7月20-21日の3時点で実施した。調査は10時から16時に実施したが、天候不良になったり、訪問客が途絶えたりすると早めに切り上げた。
-   調査員は東京農業大学生物産業学部自然資源経営学科の1～2年生で、毎回4～5名程度が参加した。
-   調査は池を囲む遊歩道の入り口付近で行い、周遊を終えた観光客に声をかけた。調査名目は、神の子池の整備に関するアンケートである。
-   ただし、後述するように7月20-21日は虫が大量発生したため一か所に留まることが難しく、遊歩道を移動して調査ポイントを時々変えながら調査を行った。

```{r date-bar}
df |> 
  ggplot(aes(x = date)) +
  geom_bar() +
  labs(title = "各日程のサンプル数")
```

-   7月20-21日が最も多い。この日は3連休（19, 20, 21日の3連休。21日は海の日で、他は土日）
-   ただし、7月20-21日は気温が上昇し、虻などの虫が池付近に大量発生していた。

## 質問項目

-   探索的調査であるため、実施日によって質問内容を一部変更している。

| 変数名 | 内容 | 備考 |
|----|----|----|
| 性別 | 男性・女性のダミー変数 |  |
| 世代 | 10代、20代、…70代以上の7区分 |  |
| 道内居住 | 居住地が道内か道外かを表すダミー変数 |  |
| 北海道訪問回数 | 1回目、2回目、3回目、4回目以上の4区分 |  |
| 同行者人数 | 1人、2人、…5人以上の5区分 |  |
| 神の子池訪問回数 |  |  |
| 訪問きっかけ |  | 7月20-21日調査票では未記載 |
| 神の子池への満足度 |  |  |
| 市民選好フレーミング |  |  |

-   なお、神の子池への再訪意図についても4つの選択肢を設定して尋ねたが、8割以上の回答者が「近くに来る機会があったら再訪したい」を選択し、ばらつきが見られないため、分析には用いない。（他の選択肢は「すでに再訪の予定がある」、「近くに来る機会を積極的に作って再訪したい」、「近くに来る機会があっても再訪したいとは思わない」）。

## 二段階二肢選択法

-   

## 二段階質問の誤回答

-   調査では二段階二肢選択形式で回答を求めたが、1段階目で未回答であったり、2段階目の分岐後の質問を両方回答したりする等、誤回答が多かった。

```{r full_wtp_right_prop-date}
df |> 
  group_by(date) |> 
  summarise(
    prop = mean(full_wtp_right)
  ) |> 
  ggplot(aes(x = date, y = prop)) +
  geom_col() +
  scale_y_continuous(
    limits = c(0, 1),
    breaks = seq(0, 1, by = 0.1)) +
  labs(title = "CVM質問に適切に回答したサンプルの割合")

```

-   各調査日程で適切に回答したサンプルの割合は70%を割っており、とくに7月20-21日は60％近くまで落ち込んでいる。当日は大量発生した虫が訪問客にもまとわりついていたため、集中力が落ちて誤回答が増えた可能性がある。

```{r missing_pattern-by_date}
df |> 
  filter(is.na(wtp_interval)) |> 
  pivot_longer(
    cols = c("bid1_right_bid2_missing","bid1_right_bid2_both_answer", "bid1_missing_only", "bid1_missing_bid2_both_answer", "full_wtp_missing"),
    names_to = "na_pattern",
    values_to = "yes"
  ) |> 
  filter(yes == 1) |> 
  group_by(date) |> 
  count(na_pattern) |> 
  summarise(na_pattern = na_pattern, prop = n / sum(n)) |> 
  ggplot(aes(y = na_pattern, x = prop)) + 
  geom_col() +
  labs(title = "欠損の内訳") +
  facet_wrap(~date)
```

-   欠損の内訳をみると、bid1_missing_only（1段階目は未回答で2段階目は適切に回答）が6割程度である。調査票では1段階目と区別するために2段階目の質問を枠で囲んだが、それにより1段階目が目立たなくなり、見過ごされた可能性がある。
    -   このパターンの欠損に関しては、たとえば１段階目でyesを回答した場合の分岐ルートで２段階目に回答しているのであれば、1段階目はyesを回答しているとみなすといった解釈は可能であるが、そのような解釈は今回は取らないこととする。なぜなら、回答者が１段階目の問題を読んだ上で回答を省略しているのか、１段階目に気づかず２段階目に回答しているのか判別できないからである。
-   また、bid1_missing_bid2_both_answer(1回目は未回答で2段階目は分岐後の質問に両方回答)は7月20-21日が欠損の3割近くを占めるが6月8日は1割に満たない。質問票の当該箇所のレイアウトは同じであるため、レイアウトによる影響とは考えにくい。虫の大量発生などの環境要因が考えられるが、特定は難しい。
-   以下の分析ではCVM質問に適切に回答したサンプルのみを用いることにする。

# 結果

## 基本属性情報

-   まずは基本属性情報を示す。分析にはCVM適切回答者のデータのみを用いるが、データの限定による偏りの発生をチェックするため、全サンプルの基本属性情報と併記して示す。

### 性別

```{r v1}
df |> 
  filter(!is.na(v1_sex)) |> 
  count(date, v1_sex) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v1_sex)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "性別割合（全サンプル）")

```

```{r v1-wtp_right}
df |> 
  filter(!is.na(v1_sex) & full_wtp_right == 1) |> 
  count(date, v1_sex) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v1_sex)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "性別割合（CVM適切回答者のみ）")

```

-   男性女性は同程度。

### 世代

```{r v2-date-prop}
df |> 
  filter(!is.na(v2_gen)) |> 
  count(date, v2_gen) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v2_gen)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "世代割合（全サンプル）")
```

```{r v2-date-prop-wtp_right}
df |> 
  filter(!is.na(v2_gen) & full_wtp_right == 1) |> 
  count(date, v2_gen) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v2_gen)) +
  geom_col() +
  facet_wrap(~date) + 
  labs(title = "世代割合（CVM適切回答者のみ）")
```

-   年代は日にちによって傾向が異なる。以下のように、6月8日は60代が25%程度でもっとも多いが、7月20-21日になると50代が30%程度でもっとも多くなる。7月は3連休に調査したため、仕事のある50代が訪れやすくなった可能性がある。20代は一貫して多めであり、時間に余裕がある学生や独身者が多いのためではないかと推察される。

### 居住地

```{r v3-date-prop}
df |> 
  filter(!is.na(v3_hokkaido)) |> 
  count(v3_hokkaido, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v3_hokkaido)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "居住地（全サンプル）")
```

```{r  v3-date-prop-wtp_right}
df |> 
  filter(!is.na(v3_hokkaido) & full_wtp_right == 1) |> 
  count(v3_hokkaido, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v3_hokkaido)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "居住地（CVM適切回答者のみ）")
```

-   居住地は、6月は道内のほうがやや多いが、7月は道外のほうがやや多くなる。これも、7月3連休の方が仕事のある人が訪問しやすくなるという考察と整合的である。

### 北海道訪問回数

```{r v4-date-prop}
df |> 
  filter(!is.na(v4_hokkaido_visits) & v3_hokkaido == "道外") |> 
  count(v4_hokkaido_visits, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v4_hokkaido_visits)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "北海道訪問回数（全サンプル）")
```

```{r v4-date-prop-wtp_right}
df |> 
  filter(!is.na(v4_hokkaido_visits) & v3_hokkaido == "道外" & full_wtp_right == 1) |> 
  count(v4_hokkaido_visits, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v4_hokkaido_visits)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "北海道訪問回数（CVM適切回答者のみ）")
```

-   北海道訪問回数は、4回目以上がもっとも多い。神の子池は北海道旅行をある程度経験した人が訪れる観光地であるといえる。ただし、7月の3連休になると3回目以下の割合がやや多くなる。連休でスケジュールに余裕があるため、他の主要な観光地とともに旅程に組み込みやすくなったのだと推測される。
    -   7月データに関しては、CVM適切回答者の方が「3回目」の割合が10％多めになっている。

### 何人連れか

```{r v5-date-prop}
df |> 
  filter(!is.na(v5_companions_cnt)) |> 
  count(v5_companions_cnt, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v5_companions_cnt)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "何人連れか（全サンプル）")
```

```{r v5-date-prop-wtp_right}
df |> 
  filter(!is.na(v5_companions_cnt) & full_wtp_right == 1) |> 
  count(v5_companions_cnt, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v5_companions_cnt)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "何人連れか（CVM適切回答者のみ）")
```

-   どの日程でも2人連れが多い。ただし、7月の3連休には、3人連れ・4人連れもやや多くなっている。

### 神の子訪問回数

```{r v6-date-prop}
df |> 
  filter(!is.na(v6_kaminoko_visits)) |> 
  count(v6_kaminoko_visits, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v6_kaminoko_visits)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "神の子訪問回数（全サンプル）")
```

```{r v6-date-prop-wtp_right}
df |> 
  filter(!is.na(v6_kaminoko_visits) & full_wtp_right == 1) |> 
  count(v6_kaminoko_visits, date) |> 
  group_by(date) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ggplot(aes(x = prop, y = v6_kaminoko_visits)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "神の子訪問回数（CVM適切回答者のみ）")
```

-   「はじめて」という回答がどの日程でも６〜７割を占める。神の子池はリピーターを生むような観光地ではない傾向があるといえる。

### 訪問きっかけ

```{r v6-date}
df |> 
  filter(date != "7月20-21日" &  v6_kaminoko_visits == "はじめて") |> 
  group_by(date) |> 
  summarise(
    web = mean(v7_web, na.rm = TRUE),
    other_web = mean(v7_other_web, na.rm = TRUE),
    web_reviews = mean(v7_web_reviews, na.rm = TRUE),
    tv = mean(v7_tv, na.rm = TRUE),
    travel_agency = mean(v7_travel_agency, na.rm = TRUE),
    magazine = mean(v7_magazine, na.rm = TRUE),
    companion_suggestion = mean(v7_companion_suggestion, na.rm = TRUE),
    non_companion_info = mean(v7_non_companion_info, na.rm = TRUE),
    .groups = "drop"
  ) |> 
  pivot_longer(
    -date,
    names_to = "choice",
    values_to = "prop"
  ) |> 
  ggplot(aes(x = prop, y = choice)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "神の子訪問きっかけ（全サンプル）")
```

```{r v6-date-wtp_right}
df |> 
  filter(date != "7月20-21日" & v6_kaminoko_visits == "はじめて" & full_wtp_right == 1) |> 
  group_by(date) |> 
  summarise(
    web = mean(v7_web, na.rm = TRUE),
    other_web = mean(v7_other_web, na.rm = TRUE),
    web_reviews = mean(v7_web_reviews, na.rm = TRUE),
    tv = mean(v7_tv, na.rm = TRUE),
    travel_agency = mean(v7_travel_agency, na.rm = TRUE),
    magazine = mean(v7_magazine, na.rm = TRUE),
    companion_suggestion = mean(v7_companion_suggestion, na.rm = TRUE),
    non_companion_info = mean(v7_non_companion_info, na.rm = TRUE),
    .groups = "drop"
  ) |> 
  pivot_longer(
    -date,
    names_to = "choice",
    values_to = "prop"
  ) |> 
  ggplot(aes(x = prop, y = choice)) +
  geom_col() +
  facet_wrap(~date) +
  labs(title = "神の子訪問きっかけ（CVM適切回答者のみ）")
```

-   ７月はこの質問を省略しているため６月のデータのみを示す。
-   6月14-15日は同行者の提案、雑誌がやや多めになる。

```{r v6-v3}
df |> 
  filter(!is.na(v3_hokkaido) & v6_kaminoko_visits == "はじめて") |> 
  group_by(v3_hokkaido) |> 
  summarise(
    web = mean(v7_web, na.rm = TRUE),
    other_web = mean(v7_other_web, na.rm = TRUE),
    web_reviews = mean(v7_web_reviews, na.rm = TRUE),
    tv = mean(v7_tv, na.rm = TRUE),
    travel_agency = mean(v7_travel_agency, na.rm = TRUE),
    magazine = mean(v7_magazine, na.rm = TRUE),
    companion_suggestion = mean(v7_companion_suggestion, na.rm = TRUE),
    non_companion_info = mean(v7_non_companion_info, na.rm = TRUE),
    .groups = "drop"
  ) |> 
  pivot_longer(
    -v3_hokkaido,
    names_to = "choice",
    values_to = "prop"
  ) |> 
  ggplot(aes(x = prop, y = choice)) +
  geom_col() +
  facet_wrap(~v3_hokkaido) +
  labs(title = "神の子訪問きっかけ・道内外比較（全サンプル）")
```

```{r v6-v3-wtp_right}
df |> 
  filter(!is.na(v3_hokkaido) & v6_kaminoko_visits == "はじめて" & full_wtp_right == 1) |> 
  group_by(v3_hokkaido) |> 
  summarise(
    web = mean(v7_web, na.rm = TRUE),
    other_web = mean(v7_other_web, na.rm = TRUE),
    web_reviews = mean(v7_web_reviews, na.rm = TRUE),
    tv = mean(v7_tv, na.rm = TRUE),
    travel_agency = mean(v7_travel_agency, na.rm = TRUE),
    magazine = mean(v7_magazine, na.rm = TRUE),
    companion_suggestion = mean(v7_companion_suggestion, na.rm = TRUE),
    non_companion_info = mean(v7_non_companion_info, na.rm = TRUE),
    .groups = "drop"
  ) |> 
  pivot_longer(
    -v3_hokkaido,
    names_to = "choice",
    values_to = "prop"
  ) |> 
  ggplot(aes(x = prop, y = choice)) +
  geom_col() +
  facet_wrap(~v3_hokkaido) +
  labs(title = "神の子訪問きっかけ・道内外比較（CVM適切回答者のみ）")
```

-   神の子訪問きっかけは、道内はネットの口コミ、同行者の提案が多い。道外は雑誌がやや多い。

### 神の子池への満足度

```{r v9-boxplot}
df |> 
  pivot_longer(
    cols = starts_with("v9_"),
    names_to = "satis_content",
    values_to = "value"
  ) |> 
  ggplot(aes(x = value, y = satis_content)) + 
  geom_boxplot(na.rm = TRUE) +
  scale_x_continuous(breaks = 1:10) + 
  labs(title = "神の子訪問満足度（全サンプル）")
```

```{r v9-boxplot-wtp_right}
df |> 
  filter(full_wtp_right == 1) |> 
  pivot_longer(
    cols = starts_with("v9_"),
    names_to = "satis_content",
    values_to = "value"
  ) |> 
  ggplot(aes(x = value, y = satis_content)) + 
  geom_boxplot(na.rm = TRUE) +
  scale_x_continuous(breaks = 1:10) + 
  labs(title = "神の子訪問満足度（CVM適切回答者のみ）")
```

-   景色満足度がもっとも高く、次に総合満足度が高い(これらは天井効果が出ているように思われる)。それ以外の満足度は低く、とくに施設満足度は低い。

```{r v9-boxplot-date-wtp_right}
df |> 
  filter(full_wtp_right == 1) |> 
  pivot_longer(
    cols = starts_with("v9_"),
    names_to = "satis_content",
    values_to = "value"
  ) |> 
  ggplot(aes(x = value, y = satis_content)) + 
  geom_boxplot(na.rm = TRUE) +
  scale_x_continuous(breaks = 1:10) + 
  facet_wrap(~date) +
  labs(title = "神の子訪問満足度・調査日別（CVM適切回答者のみ）")
```

-   調査日別の満足度の傾向を示した。総合満足度の天井効果が目立つのは６月14-15日。7月20-21日は虫が大量発生したが、満足度が大きく低下しているようには見えない。

```{r v9-boxplot-v3-wtp_right}
df |> 
  filter(full_wtp_right == 1 & !is.na(v3_hokkaido)) |> 
  pivot_longer(
    cols = starts_with("v9_"),
    names_to = "satis_content",
    values_to = "value"
  ) |> 
  ggplot(aes(x = value, y = satis_content)) + 
  geom_boxplot(na.rm = TRUE) +
  scale_x_continuous(breaks = 1:10) + 
  facet_wrap(~v3_hokkaido) +
  labs(title = "神の子訪問満足度・居住地別（CVM適切回答者のみ）")
```

-   総合満足度については道内居住者の方が天井効果を起こしている。道外居住者は満足度8のあたりに集中する傾向。

### 神の子池のどの側面に満足しているか（７月のみ）

```{r v10-boxplot}
df |> 
  pivot_longer(
    cols = starts_with("v10_"),
    names_to = "satis_content",
    values_to = "value"
  ) |> 
  ggplot(aes(x = value, y = satis_content)) + 
  geom_boxplot(na.rm = TRUE) +
  scale_x_continuous(breaks = 1:10) + 
  labs(title = "神の子の側面別満足度（全サンプル）")
```

```{r v10-boxplot-wtp_right}
df |> 
  filter(full_wtp_right == 1) |> 
  pivot_longer(
    cols = starts_with("v10_"),
    names_to = "satis_content",
    values_to = "value"
  ) |> 
  ggplot(aes(x = value, y = satis_content)) + 
  geom_boxplot(na.rm = TRUE) +
  scale_x_continuous(breaks = 1:10) + 
  labs(title = "神の子の側面別満足度（CVM適切回答者のみ）")
```

-   どれも天井効果を起こしている。特に「池自体の美しさへの満足度」「周囲の森の豊かさへの満足度」は中央値が最大の10となっており、変数としてはあまり有用でない可能性がある。(→だから使わない)


### 交流意図(７月調査以外)

```{r v12}
df |> 
  filter(date != "7月20-21日") |> 
  pivot_longer(
    cols = c("v12_interact_by_event", "v12_interact_by_market", "v12_interact_by_guesthouse", "v12_unwilling_to_interact", "v12_unaware_of_location"),
    names_to = "interact",
    values_to = "yes"
  ) |> 
  filter(!is.na(yes)) |> 
  count(interact, yes) |> 
  group_by(interact) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  filter(yes == 1) |> 
  ggplot(aes(x = prop, y = interact)) +
  geom_col() +
  scale_x_continuous(breaks = seq(0, 0.6, by = 0.05)) +
  labs(title = "交流意図割合（全サンプル）")
```

```{r v12-wtp_right}
df |> 
  filter(date != "7月20-21日" & full_wtp_right == 1) |> 
  pivot_longer(
    cols = c("v12_interact_by_event", "v12_interact_by_market", "v12_interact_by_guesthouse", "v12_unwilling_to_interact", "v12_unaware_of_location"),
    names_to = "interact",
    values_to = "yes"
  ) |> 
  filter(!is.na(yes)) |> 
  count(interact, yes) |> 
  group_by(interact) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  filter(yes == 1) |> 
  ggplot(aes(x = prop, y = interact)) +
  geom_col() +
  scale_x_continuous(breaks = seq(0, 0.6, by = 0.05)) +
  labs(title = "交流意図割合（CVM適切回答者のみ）")
```

-   直売で商品を見てみたいという人が半数以上。交流したくない人は15%程度、神の子池が清里町にあると意識していなかった人が20%。

```{r v12-v5-wtp_right}
df |> 
  filter(date != "7月20-21日" & full_wtp_right == 1 & !is.na(v3_hokkaido)) |> 
  pivot_longer(
    cols = c("v12_interact_by_event", "v12_interact_by_market", "v12_interact_by_guesthouse", "v12_unwilling_to_interact", "v12_unaware_of_location"),
    names_to = "interact",
    values_to = "yes"
  ) |> 
  filter(!is.na(yes)) |> 
  count(v3_hokkaido, interact, yes) |> 
  group_by(interact, v3_hokkaido) |> 
  mutate(
    prop = n / sum(n)
  ) |> 
  ungroup() |> 
  filter(yes == 1) |> 
  ggplot(aes(x = prop, y = interact)) +
  geom_col() +
  scale_x_continuous(breaks = seq(0, 0.6, by = 0.10)) +
  labs(title = "交流意図割合・居住地別（CVM適切回答者のみ）") +
  facet_wrap(~v3_hokkaido)
```

-   神の子池が清里町にあると意識していなかったという人は道外だと25%に達する。また、イベントで交流したいという人は道外は少なめ（10%）になる。


### 支払意思

```{r yes_no_pattern}
df |> 
  count(v14_bid1_wtp, yes_no_pattern) |> 
  pivot_wider(names_from = yes_no_pattern, values_from = n)
```
```{r yes_no_pattern-percent}
df |> 
  count(v14_bid1_wtp, yes_no_pattern) |>
  mutate(yes_no_pattern = ifelse(is.na(yes_no_pattern), "na", yes_no_pattern)) |> 
  group_by(v14_bid1_wtp) |> 
  mutate(percent = round(n / sum(n)  * 100, 1)) |>  # パーセント表記
  pivot_wider(
    id_cols = v14_bid1_wtp,
    names_from = yes_no_pattern, 
    values_from = percent)
```
- パーセントで回答割合を表記した。
- yyは一段階目提示金額が大きくなるほど割合が減っている。これは正常。
- nnは基本的に一段階目提示金額が大きいほど割合が増えるが、500円から1000円に移行する段階で割合が減る。一段階目提示金額が1000円の場合、nyが26.0%でもっとも割合が大きい。初回で大きめの金額を提示すると、それを断った回答者にとっては、二段階目の提示金額500円が安いと感じやすくなるのではないか。つまり、一段階目から二段階目への金額減少幅が「500円」とかなり大きいので、アンカリング効果により安いと感じ、受け入れやすくなったのではないか。
- 欠損は一段階目提示額100円と1000円のそれぞれでもっとも大きく、40%に達する。金額が低すぎても高すぎても回答者は真面目に回答する動機を失うということだろうか。


```{r v14_bid1_distribution}
df |> 
  filter(!is.na(v14_bid1) & !is.na(wtp_interval)) |> 
  group_by(v14_bid1_wtp) |> 
  summarise(
    yes_rate = mean(v14_bid1 == "支払う"),
    n = n(),
    yes_n = round(n * yes_rate)
  ) |> 
  ggplot(aes(x = v14_bid1_wtp, y = yes_rate)) +
  geom_point(size = 2) +
  geom_line() +
  geom_text(aes(label = paste0(yes_n, "/", n, "人")), vjust = -0.7, size = 3) +
  scale_x_continuous(breaks = seq(0, 1000, by = 100)) +
  scale_y_continuous(labels = scales::percent) +
  coord_cartesian(clip = "off") +
  theme(plot.margin = margin(5.5, 20, 5.5, 5.5)) +
  labs(title = "一段階目提示金額に対するyes割合の分布（CVM適切回答者のみ）")
```
一段階目提示金額に対するyes率分布。きれいに右下がりになっている

```{r v14_bid2_yes_distribution}
df |> 
  filter(!is.na(v14_bid2_yes) & !is.na(wtp_interval)) |> 
  group_by(v14_bid2_yes_wtp) |> 
  summarise(
    yes_rate = mean(v14_bid2_yes == "支払う"),
    n = n(),
    yes_n = round(n * yes_rate)
  ) |> 
  ggplot(aes(x = v14_bid2_yes_wtp, y = yes_rate)) +
  geom_point(size = 2) +
  geom_line() +
  geom_text(aes(label = paste0(yes_n, "/", n, "人")), vjust = -0.7, size = 3) +
  scale_x_continuous(breaks = seq(0, 1500, by = 100)) + 
  scale_y_continuous(labels = scales::percent) +
  coord_cartesian(clip = "off") +
  theme(plot.margin = margin(5.5, 20, 5.5, 5.5)) +
  labs(title = "二段階目提示金額に対するyes割合の分布（一段階目yes & CVM適切回答者のみ）")
```

- 二段階目（一段階目yes回答者のみ）のyes率分布。1000円までは右下がりだが、1500円で逆に右上がりになっている。
- 1500円は回答者が33人であり、他の金額の場合の半分程度である。二段階目1500円の回答者の初回提示金額は1000円である。多くの回答者が1000円の提示金額に対してnoと回答したため、1500円の回答者が減少したものと思われる。回答者が少ないため、yes率が不安定になった可能性がある。
- 別の解釈としては、一段階目提示金額から二段階目に移行する際の金額変化の大きさが影響している可能性がある。一段階目でyesと回答した場合、二段階目への金額変化は「500→1000」「1000→1500」である。1000円から1500円への変化は1.5倍だが、500円から1000円への変化は2倍である。変化としては500円から1000円への変化の方が大きく感じられ、yes率が低くなったのかもしれない。ただし、低金額帯では100円から300円と3倍変化していてもyes率がそれほど低くなっておらず、変化割合と金額の組み合わせで反応が異なっている可能性も考えられる。

```{r v14_bid2_no_distribution}
df |> 
  filter(!is.na(v14_bid2_no) & !is.na(wtp_interval)) |> 
  group_by(v14_bid2_no_wtp) |> 
  summarise(
    yes_rate = mean(v14_bid2_no == "支払う"),
    n = n(),
    yes_n = round(yes_rate * n)
  ) |> 
  ggplot(aes(x = v14_bid2_no_wtp, y = yes_rate)) +
  geom_point( size = 2) +
  geom_line() +
  geom_text(aes(label = paste0(yes_n, "/", n, "人")), vjust = -0.7, size = 3) +
  scale_x_continuous(breaks = seq(0, 1500, by = 100)) + 
  scale_y_continuous(labels = scales::percent)  +
  coord_cartesian(clip = "off") +
  theme(plot.margin = margin(5.5, 20, 5.5, 5.5))+
  labs(title = "二段階目提示金額に対するyes割合の分布（一段階目no & CVM適切回答者のみ）")

```

- 一段階目noの回答者の二段階目のyes率を示した分布である。右下がりからかなり崩れている。低価格帯では回答者自体がかなり少ないので、yes率が不安定になっていると思われる。
- 500円では回答者の数が41名とそれなりに多い。最高金額であるにも関わらずyes率がもっとも大きいのは、他の価格帯の回答者数が少なく不安定な結果であるためであるとも解釈できる。別の解釈としては、一段階目で1000円を提示されてnoと回答し、二段階目で500円を提示された回答者にとっては、1000円から500円への金額の減少幅が比較的大きく感じられyesと回答しやすくなったというものが考えられる。いずれにしても全体的に回答者数が少ないので解釈は慎重にするべきである。


## 訪問満足度の規定要因

```{r v9-regression}

result <- lm(v9_general_satisfaction ~ v9_scenery_satisfaction + v9_access_satisfaction + v9_facility_satisfaction + v9_tourist_spot_satisfaction, data = df) 

result |> summary()
```

```{r v9-regression-wtp_right}

result <- lm(v9_general_satisfaction ~ v9_scenery_satisfaction + v9_access_satisfaction + v9_facility_satisfaction + v9_tourist_spot_satisfaction, data = df |> filter(full_wtp_right == 1) )

result |> summary()
```
- 総合満足度は景色満足度でだいたい決まってくる（景色満足度が1大きくなると総合満足度が0.68大きくなる）。決定係数も0.678と大きい。
- 次に大きいのは周囲の観光スポットの満足度。
- アクセス満足度も有意ではあるが、係数の大きさは景色満足度の10％程度である（アクセス満足度が1大きくなっても、総合満足度は0.067しか大きくならない）
- 施設満足度は全サンプルを入れた分析では10%水準有意だが、CVM質問に適切に回答しているサンプルのみを使うと有意判定がはずれる。また、いずれにしても係数はアクセス満足度よりさらに小さい。
- したがって、整備によりアクセスや施設を改善し訪問者の利便性を高めたとしても、訪問の総合満足度にはあまり寄与しないと考えられる。
- ただし、景色満足度と総合満足度は天井効果が発生しているように思われる。これらをダミー化して分析し直してみる（10であれば1, 1～9であれば0とする）。

```{r v9_dummy-logit_regression-wtp_right}

df2 <- df |> 
  mutate(
    general_dummy = case_when(
      v9_general_satisfaction == 10 ~ 1,
      v9_general_satisfaction %in% 1:9 ~ 0,
      is.na(v9_general_satisfaction) ~ NA_integer_
    ),
    scenery_dummy  = case_when(
      v9_scenery_satisfaction == 10 ~ 1,
      v9_scenery_satisfaction %in% 1:9 ~ 0,
      is.na(v9_scenery_satisfaction) ~ NA_integer_)
  )


result <- lm(general_dummy ~ scenery_dummy + v9_access_satisfaction + v9_facility_satisfaction + v9_tourist_spot_satisfaction, data = df2 |> filter(full_wtp_right == 1) ,
             family = binomial(link = "Logit"))

result |> summary()

```

総合満足度と景色満足度をダミー変数化してロジスティック回帰。景色は有意のままで、他の変数の有意判定が変わっている。

```{r odds-v9_dummy-logit_regression-wtp_right}
exp(coef(result))
```

オッズ比。景色ダミー以外はだいたい1のあたりなので、総合満足度に影響しない傾向。やはり景色満足度で総合満足度はだいたい決まってしまうようだ。

## 手つかず志向指標の作成


