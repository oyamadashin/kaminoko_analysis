---
title: "kaminoko_2025_eda"
format: html
execute:
  freeze: auto
  warning: false
---

## パッケージ読み込み



```{r}
#| include: false
library(tidyverse)
library(patchwork)
```

## データインポート

```{r}
#| include: false

df <- read_csv("kaminoko2025_all.csv")


```

## 変数名変更or再コーディング

```{r}
# 変数名を間違って入力していたので修正。
df <- df |> 
  rename_with(~ gsub("^q12", "v12", .x)) 

df <- df |> 
  rename_with(~ gsub("^q14", "v14", .x)) 

# 北海道内（外）と道内（外）が混じっていたので道内（外）に統一
df <- df |> 
  mutate(
    v3_hokkaido = case_when(
      v3_hokkaido == "北海道内" ~ "道内",
      v3_hokkaido == "北海道外" ~ "道外",
      TRUE ~ v3_hokkaido
    )
  )

# 日付をファクタ化してレベル設定
df <- df |> 
  mutate(
    date = factor(date, levels = c("6月8日", "6月14-15日", "7月19-20日"))
  )

# 1,2のままだと使いにくいので、カテゴリー名をかえる。
df <- df |> 
  mutate(
    v13_citizen_consumer = case_when(
      v13_citizen_consumer == 1 ~ "市民選好",
      v13_citizen_consumer == 2 ~ "消費者選好",
      TRUE ~ NA_character_
    )
  )
```

# EDA

## なぜ20代が多い？

```{r}
df |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_wrap(~date)
```

-   このように、20代がなぜか多いため、分布がフタコブになっている。

```{r}
df |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_wrap(~v3_hokkaido)
```

-   道外からの来訪者の方が50代以上が多い。一方、道内からの来訪者は20代が多い　→　道外から来るのは時間だけでなくお金もある年配の世代。一方、時間があってもお金のない若い世代は道内の無料観光スポットとして神の子池を訪れているのでは？
-   世代による観光様式の違いがフタコブ分布の原因ではないだろうか。

[25] 道外からの来訪者の方が50代以上が多い。一方、道内からの来訪者は20代が多い

```{r}
df |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v3_hokkaido)
```

-   7月19～21日は海の日で3連休だ。道外からの50代層は仕事があるのでこの3連休をねらって北海道旅行を計画していたのだろう。そのため、7月19～20日のデータでは道外の50代が非常に多くなっている。

[26] 7月19～20日のデータでは道外の50代が非常に多くなっている

### TODO

-   となると、20代はWTPが低い傾向なのでは。
-   道外の50代以上層がどんな人たちなのか、もう少し分析の余地はあるな。同行者の人数とかも考慮しようか。

### 結論(仮)

-   20代の動向は50代の動向とセットで考えるべき。道外から来るのは時間だけでなくお金もある年配の世代。一方、時間があってもお金のない若い世代は道内の無料観光スポットとして神の子池を訪れているのでは？つまり、観光様式の違いがフタコブ分布を生み出している。
-   また、道外50代は7月の3連休に非常に多く来ている。彼らは仕事があるので、休日の状況によって行動を敏感に変える。一方、道内の50代にはそうした休日状況による行動のちがいはみられない。

## それぞれの世代はどんなタイプの人たち？

### 世代×同行者人数

-   まず、それぞれの世代によって同行者の人数がどうちがうかをみてみよう。

```{r}
df |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v5_companions_cnt)
```

-   7月の3連休には50代、60代で1人旅という人が少しいる。彼らが道内か道外かですこしイメージが変わってくると思う。50代、60代の人がひとりでふらりと近場の観光地を訪れる、というのは、既婚者でもありうる気がする。しかし、道外からひとりで神の子池まで来る50代、60代というのは、少し変わった人ではないだろうか？→こういう人たちは神の子池に来るまでの「達成感」を重視するのでは。
-   20代は3人連れ、4人連れという人もやや多い。大学生グループ、あるいは独身の若者グループだろうか。
- 7月の連休で、40代の4人連れがやや多いのは、家族連れなのかもしれない(といっても、数自体はかなり少ないが)。
-   50代は2人連れの人が多い（とくに7月の3連休）。子供に手がかからなくなってきたころの年配夫婦というイメージだろうか。

[27] 7月の3連休には50代、60代で1人旅という人が少しいる
[28] 20代は3人連れ、4人連れという人もやや多い

### 世代×来訪きっかけ

#### 町・観光協会のサイトがきっかけ

```{r}
df |> 
  filter(!is.na(v7_web)) |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v7_web)
```

50代でわずかに町・観光協会のサイトがきっかけという人はいる。でも10人にも満たない。→そもそも神の子池の存在を知らなければ清里町のような小さな町のサイトを見ることもないだろう。だからサイトがきっかけにならないのは当たり前だ。

#### ネットの口コミがきっかけ

```{r}
df |> 
  filter(!is.na(v7_web_reviews)) |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v7_web_reviews)
```

20代と50代が拮抗している。70代以上を除き、あまり世代差は無い感じ。だいたいどの世代もネット口コミで来ているのだろうか。

[29] 10代、70代以上を除き、ネット口コミがきっかけで来訪した人はどの世代も多い。

#### 雑誌やガイドブックがきっかけ

```{r}
df |> 
  filter(!is.na(v7_magazine)) |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v7_magazine)
```

雑誌は60代がやや多い。ここでも70代以上はほとんどいない。（70代以上と10代は誰かに連れてこられたというパターンが多いのかもしれない）
ただ、いずれにしても雑誌は全体的に少数派。

#### 同行者の提案

```{r}
df |> 
  filter(!is.na(v7_companion_suggestion)) |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v7_companion_suggestion)
```

あれ？意外と10代、70代以上は同行者の提案という人が少ない　→　彼らは何がきっかけで来たの？　→　ただ、これでも他のきっかけに比べると数は多い（そもそもこれらの年代は該当者が少ない）

[30] 20代は同行者の提案で来る人が非常に多い。

#### 非同行者の話がきっかけ

```{r}
df |> 
  filter(!is.na(v7_non_companion_info)) |> 
  ggplot(aes(y = v2_gen, fill = v2_gen)) +
  geom_bar() +
  facet_grid(date~v7_non_companion_info)
```

非同行者の話を聞いて、という人は50代が多め（いずれにしても数がすくない）。

#### 年代できっかけがどうちがうかの見取り図

```{r}
df |> 
  filter(!is.na(v2_gen)) |> 
  group_by(v2_gen) |> 
  summarise(
    web = sum(v7_web, na.rm = TRUE),
    other_web = sum(v7_other_web, na.rm = TRUE),
    web_reviews = sum(v7_web_reviews, na.rm = TRUE),
    tv = sum(v7_tv, na.rm = TRUE),
    travel_agency = sum(v7_travel_agency, na.rm = TRUE),
    magazine = sum(v7_magazine, na.rm = TRUE),
    companion_suggestion = sum(v7_companion_suggestion, na.rm = TRUE),
    non_companion_info = sum(v7_non_companion_info, na.rm = TRUE),
    .groups = "drop"
  ) |> 
  pivot_longer(
    -v2_gen,
    names_to = "choice",
    values_to = "count"
  ) |> 
  ggplot(aes(x = count, y = choice)) +
  geom_col() +
  facet_wrap(~v2_gen)
```

10代、70代以上はそもそも回答者が少ない。そして、回答のバリエーションが少ない（情報源が少ないということか）。

30代から60代は分布が似ている。20代だけがやはり変わっていて、同行者の提案という人が非常に多く、ネット口コミもそこそこ多い。

20代はグループで来てるパターンが多いのではないだろうか。その場合、同行者の提案という人が多くなるのは当たり前だ。　→　あ、すでに求めていた。20代は7月になると同行者数が多くなる。三連休でグループ旅行に来てたという事だろう。

#### 同行者の人数が多い方が、同行者の提案がきっかけという人は多いのか？

```{r}
df |> 
  filter(!is.na(v5_companions_cnt)) |> 
  group_by(v5_companions_cnt) |> 
  summarise(
    web = sum(v7_web, na.rm = TRUE),
    other_web = sum(v7_other_web, na.rm = TRUE),
    web_reviews = sum(v7_web_reviews, na.rm = TRUE),
    tv = sum(v7_tv, na.rm = TRUE),
    travel_agency = sum(v7_travel_agency, na.rm = TRUE),
    magazine = sum(v7_magazine, na.rm = TRUE),
    companion_suggestion = sum(v7_companion_suggestion, na.rm = TRUE),
    non_companion_info = sum(v7_non_companion_info, na.rm = TRUE),
    .groups = "drop"
  ) |> 
  pivot_longer(
    -v5_companions_cnt,
    names_to = "choice",
    values_to = "count"
  ) |> 
  ggplot(aes(x = count, y = choice)) +
  geom_col() +
  facet_wrap(~v5_companions_cnt)
```

3人連れ以上だと同行者の提案がきっかけという人が最も多くなる。

→　グループ旅行をする場合、グループの誰かが行先を提案して、他の人はそれについていくだけ、というパターンが多いだろう（いちいちみんなで相談して決めるということはあまりしない）。20代はまだ独身で10代よりも自由に行動できる傾向があるため、グループ旅行をする傾向が高いのではないか。だから、20代で「同行者の提案」がきっかけの人が多くなるのだろう。

また、1人旅だと非同行者の話がきっかけという人が少ないのは、彼らが能動的に情報を集める傾向があるからではないか。

## 期待度関連

-   神の子池に来るまでのコストが高い人の方が、その分、神の子池に対する期待度が高いのではないだろうか。そうでなければそもそもコストを支払おうとしないだろう。

    -   これを検証するには？

    -   コストに該当するのは居住地「道内・道外」か、だろう。道外の人の方がコストが高い。また、「同行者の提案がきっかけ」という人は受身なのでコストが低いという見方もできる。どちらもダミー変数だな。

    -   だから単純に、ダミー変数の0, 1それぞれでファセットして期待度分布の違いをみる、というのでいいと思う。あるいはcolorでグループ分けするのもOK。

### 居住地×景色期待度

```{r}
# 居住地×景色期待度で棒グラフ
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v3_hokkaido)) |> 
  group_by(v3_hokkaido) |> 
  ggplot(aes(x = v8_scenery_expectation)) +
  geom_bar() +
  facet_wrap(~v3_hokkaido)
  
```

-   あれ？　道内の方が期待度高めかな。平均とかもみないとよくわからないな。

```{r}
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v3_hokkaido)) |> 
  ggplot(aes(x = v8_scenery_expectation)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:10) +
  facet_wrap(~ v3_hokkaido)
```
道外の方が8あたりで大きくジャンプしている。一方、道内の方がばらつきがやや多い

```{r}
# 居住地×景色期待度で箱ひげ図＋バイオリンプロット
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v3_hokkaido)) |> 
  ggplot(aes(x = v3_hokkaido, y = v8_scenery_expectation)) +
  geom_violin(trim = FALSE) +
  geom_boxplot(width = 0.15, outlier.shape = NA)
```

-   期待度の中央値は同じ。高得点の方の分布はほとんど変わらない。

-   一方、道内の方が尾が長い。つまり、期待度の低い人たちが少し見られるということだ。

    -   **彼らはどういう人たちだろう？　同行者に連れてこられた人たち？　再訪経験があってあまり期待をせずに来た人たち？**

### 居住地×総合期待度

```{r}
# 居住地×総合期待度で棒グラフ

df |> 
  filter(!is.na(v8_general_expectation) & !is.na(v3_hokkaido)) |> 
  group_by(v3_hokkaido) |> 
  ggplot(aes(x = v8_general_expectation)) +
  geom_bar() +
  facet_wrap(~v3_hokkaido)
```

```{r}
df |> 
  filter(!is.na(v8_general_expectation) & !is.na(v3_hokkaido)) |> 
  ggplot(aes(x = v8_general_expectation)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:10) +
  facet_wrap(~ v3_hokkaido)
```
やはり道内の方が期待度のばらつきが多いようにみえる

```{r}
df |> 
  filter(!is.na(v8_general_expectation) & !is.na(v3_hokkaido)) |> 
  ggplot(aes(x = v3_hokkaido, y = v8_general_expectation)) +
  geom_violin(trim = FALSE) +
  geom_boxplot(width = 0.15, outlier.shape = NA)
```

-   総合期待度も似たような傾向かなあ。

### 同行者の提案×景色期待度

```{r}
# 同行者の提案×景色期待度でCDF
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v7_companion_suggestion)) |> 
  group_by(v7_companion_suggestion) |> 
  ggplot(aes(x = v8_scenery_expectation)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:10) +
  facet_wrap(~v7_companion_suggestion)
```
[43]同行者の提案がきっかけの方が景色期待度CFDのばらつきが大きい

```{r}
# 同行者の提案×景色期待度で箱ひげ図＋バイオリンプロット
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v7_companion_suggestion)) |> 
  ggplot(aes(x = as.factor(v7_companion_suggestion), y = v8_scenery_expectation)) +
  geom_violin(trim = FALSE) +
  geom_boxplot(width = 0.15, outlier.shape = NA)
```

-   同行者の提案で来た人たちの方が期待度がばらける感じ。同行者に提案されて「楽しみ！」という人もいれば「ええー。面倒だけど仕方ないなあ」という人もいる、ということか。

```{r}
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v7_companion_suggestion) & !is.na(v3_hokkaido)) |> 
  ggplot(aes(x = as.factor(v7_companion_suggestion), y = v8_scenery_expectation)) +
  geom_violin(trim = FALSE) +
  geom_boxplot(width = 0.15, outlier.shape = NA) +
  facet_wrap(~v3_hokkaido)
```

```{r}
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v7_companion_suggestion) & !is.na(v3_hokkaido)) |> 
  ggplot(aes(x = v3_hokkaido, y = v8_scenery_expectation)) +
  geom_violin(trim = FALSE) +
  geom_boxplot(width = 0.15, outlier.shape = NA) +
  facet_wrap(~as.factor(v7_companion_suggestion))
```


```{r}
# 同行者の提案×景色期待度でCDF
df |> 
  filter(!is.na(v8_scenery_expectation) & !is.na(v7_companion_suggestion) & !is.na(v3_hokkaido)) |> 
  group_by(v7_companion_suggestion) |> 
  ggplot(aes(x = v8_scenery_expectation)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:10) +
  facet_grid(v3_hokkaido~v7_companion_suggestion)
```

- 道内か道外かよりも、同行者の提案がきっかけかどうかで景色期待度CDFの形状が決まっているようだ。

```{r}
# 同行者の提案×総合期待度でCDF
df |> 
  filter(!is.na(v8_general_expectation) & !is.na(v7_companion_suggestion) & !is.na(v3_hokkaido)) |> 
  group_by(v7_companion_suggestion) |> 
  ggplot(aes(x = v8_general_expectation)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:10) +
  facet_grid(v3_hokkaido~v7_companion_suggestion)
```

- 総合期待度についてはむしろ道内か道外かの方が影響しているようにみえる。ただ、影響の方向性がいまいちつかめない。



## 自然志向は何に影響する？

### 自然志向 × 池の要素の満足度

```{r}
# v15_critical_access_attitude × v10_kaminoko_scenery_satis

df |> 
  filter(!is.na(v10_kaminoko_scenery_satis) & !is.na(v15_critical_access_attitude)) |> 
  count(v10_kaminoko_scenery_satis, v15_critical_access_attitude) |> 
  ggplot(aes(x = v15_critical_access_attitude, y = v10_kaminoko_scenery_satis, size = n)) +
  geom_point() +
  scale_x_continuous(breaks = 1:7) +
  scale_y_continuous(breaks = 1:10)
```
相関はみられない

```{r}
# v15_critical_access_attitude × v10_kaminoko_silence_satis

df |> 
  filter(!is.na(v10_kaminoko_silence_satis) & !is.na(v15_critical_access_attitude)) |> 
  count(v10_kaminoko_silence_satis, v15_critical_access_attitude) |> 
  ggplot(aes(x = v15_critical_access_attitude, y = v10_kaminoko_silence_satis, size = n)) +
  geom_point() +
  scale_x_continuous(breaks = 1:7) +
  scale_y_continuous(breaks = 1:10)
```
相関見られない

```{r}
# v15_critical_access_attitude × v10_kaminoko_forest_satis

df |> 
  filter(!is.na(v10_kaminoko_forest_satis) & !is.na(v15_critical_access_attitude)) |> 
  count(v10_kaminoko_forest_satis, v15_critical_access_attitude) |> 
  ggplot(aes(x = v15_critical_access_attitude, y = v10_kaminoko_forest_satis, size = n)) +
  geom_point() +
  scale_x_continuous(breaks = 1:7) +
  scale_y_continuous(breaks = 1:10)
```

相関はみられない


```{r}
# v15_critical_access_attitude × v10_kaminoko_achievement_satis

df |> 
  filter(!is.na(v10_kaminoko_achievement_satis) & !is.na(v15_critical_access_attitude)) |> 
  count(v10_kaminoko_achievement_satis, v15_critical_access_attitude) |> 
  ggplot(aes(x = v15_critical_access_attitude, y = v10_kaminoko_achievement_satis, size = n)) +
  geom_point() +
  scale_x_continuous(breaks = 1:7) +
  scale_y_continuous(breaks = 1:10)
```

- 微妙に相関があるような… →　相関係数出したらまったく相関なかった

```{r}
# 面倒なのでいっぺんに相関行列を出してみよう
m <- df |> 
  select(v15_critical_access_attitude, v15_critical_parking_attitude, v15_critical_crowd_attitude, v15_critical_shop_attitude, v15_critical_photospot_attitude,v10_kaminoko_scenery_satis, v10_kaminoko_silence_satis, v10_kaminoko_forest_satis, v10_kaminoko_achievement_satis) |> 
  cor(use = "pairwise.complete.obs", method="kendall")

corrplot(m)
```
うん。関係はまったくなさそう。

[44]自然志向変数と神の子要素満足度には全く相関なし

### 自然志向 × 神の子訪問満足度

```{r}
# 面倒なのでいっぺんに相関行列を出してみよう
m <- df |> 
  select(v15_critical_access_attitude, v15_critical_parking_attitude, v15_critical_crowd_attitude, v15_critical_shop_attitude, v15_critical_photospot_attitude, 
         v9_scenery_satisfaction, v9_access_satisfaction, v9_facility_satisfaction, v9_tourist_spot_satisfaction, v9_general_satisfaction) |> 
  cor(use = "pairwise.complete.obs", method="kendall")

corrplot(m)
```

これも関係なさそうだな。ただ、自然志向でクラスター分析したら訪問者のタイプ分けはできそうだ。

[45]自然志向変数と神の子訪問満足度には全く相関なし


### 年代による自然志向の違い

```{r}
df |> 
  filter(!is.na(v2_gen) & !is.na(v15_critical_access_attitude)) |> 
  ggplot(aes(x = v15_critical_access_attitude)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:7) +
  xlab("広い道路は理想の自然観光の妨げになるか？") +
  facet_wrap(~ v2_gen)
```

60代と70代以上は広い道路の整備についてどちらでもないという意見が多い。意見の多様性が低いということか？

```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  group_by(older_than_60) |> 
  summarise(
    n = sum(!is.na(v15_critical_access_attitude)),
    mean = mean(v15_critical_access_attitude, na.rm = TRUE),
    sd = sd(v15_critical_access_attitude, na.rm = TRUE)
    )
  
```
[47]60代以上は広い道路が妨げかという意見の多様性が低い

```{r}
df |> 
  filter(!is.na(v2_gen) & !is.na(v15_critical_parking_attitude)) |> 
  ggplot(aes(x = v15_critical_parking_attitude)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:7) +
  xlab("広い駐車場は理想の自然観光の妨げになるか？") +
  facet_wrap(~ v2_gen)
```
少し傾向が見えにくい

```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  group_by(older_than_60) |> 
  summarise(
    n = sum(!is.na(v15_critical_parking_attitude)),
    mean = mean(v15_critical_parking_attitude, na.rm = TRUE),
    sd = sd(v15_critical_parking_attitude, na.rm = TRUE)
    )
  
```

```{r}
df |> 
  filter(!is.na(v2_gen) & !is.na(v15_critical_crowd_attitude)) |> 
  ggplot(aes(x = v15_critical_crowd_attitude)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:7) +
  xlab("観光客の多さは理想の自然観光の妨げになるか？") +
  facet_wrap(~ v2_gen)
```
全体的に右に寄る傾向。観光客の多さはだいたいどの年代でも嫌われるよう。ただ、60代、70代以上に関してはその傾向が弱いようだ。→というより、ばらつきが小さい



```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  ggplot(aes(x = v15_critical_crowd_attitude)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:7) +
  xlab("観光客の多さは理想の自然観光の妨げになるか？") +
  facet_wrap(~ older_than_60)
```
```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  group_by(older_than_60) |> 
  summarise(
    n = sum(!is.na(v15_critical_crowd_attitude)),
    mean = mean(v15_critical_crowd_attitude, na.rm = TRUE),
    sd = sd(v15_critical_crowd_attitude, na.rm = TRUE)
    )
  
```
[46]60代以上は観光客が妨げかという意見の多様性が低い

```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  ggplot(aes(x = v15_critical_shop_attitude)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:7) +
  xlab("売店は理想の自然観光の妨げになるか？") +
  facet_wrap(~ older_than_60)
```
```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  group_by(older_than_60) |> 
  summarise(
    n = sum(!is.na(v15_critical_shop_attitude)),
    mean = mean(v15_critical_shop_attitude, na.rm = TRUE),
    sd = sd(v15_critical_shop_attitude, na.rm = TRUE)
    )
  
```
[48]売店が妨げとなるかという意見のばらつきは同程度だが60代以上の平均が高い

```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  ggplot(aes(x = v15_critical_photospot_attitude)) +
  stat_ecdf() +
  scale_x_continuous(breaks = 1:7) +
  xlab("撮影スポットは理想の自然観光の妨げになるか？") +
  facet_wrap(~ older_than_60)
```
```{r}
df |>
  filter(!is.na(v2_gen)) |> 
  mutate(
   older_than_60 = case_when(
     v2_gen %in% c("60代", "70代以上")  ~ "60代以上",
     TRUE ~ "60代未満"
   ) 
  ) |> 
  group_by(older_than_60) |> 
  summarise(
    n = sum(!is.na(v15_critical_photospot_attitude)),
    mean = mean(v15_critical_photospot_attitude, na.rm = TRUE),
    sd = sd(v15_critical_photospot_attitude, na.rm = TRUE)
    )
  
```
撮影スポットに関しても60代以上の方がばらつきが小さい


## WTP関連


